# Daily — 2026-04-30

## Hecho ayer / sesión anterior
- Reorganización docs/ completa, reescritura CLAUDE.md v3.0
- Audit profundo documentado: 9 gaps de deuda técnica en backlog

## Hoy / esta sesión (sesión 12)
- Resueltos 3 gaps de alta prioridad via subagent-driven-development:
  - Fix hidratación async App.tsx (`isHydrated` flag en authStore)
  - IDs reales post-login: `GET /users/me` backend + LoginPage/RegisterPage
  - Handler `UserCreated` registrado en event_bus (signups auditados)
- Fix extra descubierto: `RegisterRequest.name` → `username` (mismatch silencioso con backend)
- `User.name` → `User.username` en toda la capa frontend (Sidebar, DashboardPage)
- Commit: f632a20 en rama `dev`
- Nueva deuda documentada: `UserCreated` sin `created_by_user_id` para flujo admin

## Sesión 13 (continuación del día)
- Detectados 8 docs nuevos de testing en `docs/` — 4 backend (pytest) + 4 frontend (Vitest)
- Tests reales confirmados: `backend/tests/` (8 archivos) + `frontend/src/**/*.test.*` (8 archivos)
- Ejecución real: 103/117 backend ✅ · 68/71 frontend ✅ · **18 fallos totales**
- QA ejecutado → **Rechazado** hasta 0 fallos
- Issues principales: Mock.__name__ (11), require() ESM (2), Card query (1), Input password role (1), UUID version (1), bcrypt 72b (1)
- Backlog actualizado con los 8 fixes pendientes

## Bloqueos
- Tests con 18 fallos — no commiteable hasta corrección

## Contexto de los nuevos docs
Los 8 archivos de testing en `docs/` corresponden a Fase 6 QA. Su posición correcta sería `docs/developer/` pero están en raíz. Pendiente reorganización (baja prioridad). `TEST_SUMMARY.md` y `FRONTEND_TEST_SUMMARY.md` usan sintaxis Python (`"""`) — son .md con formato incorrecto.

---
*Actualizado: 2026-04-30 (sesión 13)*
